<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Update User</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body
    class="bg-zinc-900 text-white flex items-center justify-center min-h-screen"
  >
    <div class="w-full max-w-md p-6 bg-zinc-800 rounded-xl shadow-lg">
      <h1 class="text-3xl font-semibold mb-6 text-center">Update User</h1>

      <form action="/update/<%= user._id %>" method="POST" class="space-y-4">
        <input
          type="text"
          name="fullname"
          value="<%= user.fullname %>"
          placeholder="Full Name"
          class="w-full px-4 py-2 bg-transparent border-2 border-zinc-600 rounded-md outline-none placeholder:text-zinc-400"
        />

        <input
          type="text"
          name="phone"
          value="<%= user.phone %>"
          placeholder="Phone Number"
          class="w-full px-4 py-2 bg-transparent border-2 border-zinc-600 rounded-md outline-none placeholder:text-zinc-400"
        />

        <input
          type="email"
          name="email"
          value="<%= user.email %>"
          placeholder="Email Address"
          class="w-full px-4 py-2 bg-transparent border-2 border-zinc-600 rounded-md outline-none placeholder:text-zinc-400"
        />

        <input
          type="text"
          name="username"
          value="<%= user.username %>"
          placeholder="Username"
          class="w-full px-4 py-2 bg-transparent border-2 border-zinc-600 rounded-md outline-none placeholder:text-zinc-400"
        />

        <input
          type="text"
          name="image"
          value="<%= user.image %>"
          placeholder="Image URL"
          class="w-full px-4 py-2 bg-transparent border-2 border-zinc-600 rounded-md outline-none placeholder:text-zinc-400"
        />

        <button
          type="submit"
          class="w-full py-2 bg-blue-600 hover:bg-blue-700 rounded-md font-semibold transition"
        >
          Update
        </button>
      </form>
    </div>
  </body>
</html>
